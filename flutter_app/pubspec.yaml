name: school_app
description: កម្មវិធីគ្រប់គ្រងពិន្ទុសិស្ស (Admin/Teacher/Student) — Khmer UI
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1
  dio: ^5.7.0
  shared_preferences: ^2.5.4
  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/bg_school.svg
